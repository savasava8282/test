# Текущий статус проекта

## Исходная точка этой задачи

- Исходный commit задачи: `d4f455f26c59c9097c66124ae0d0ea2e67547a33`.
- Этот commit реализации геокодирования независимо проверен в GitHub.
- Перед реальным тестом HEAD совпадал с `origin/main`, рабочее дерево было чистым.
- Исходный commit реализации `/start`: `733e616b944671e736531981bd8b9c5c70dafc09`.
- Этот commit независимо проверен в GitHub.
- Рабочая ветка: `main`.
- Рабочий каталог: `/root/projects/test`.
- Рабочее дерево перед началом задачи было чистым.

## Что уже сделано

- Сохранена минимальная Python-основа на стандартной библиотеке.
- Сохранён безопасный слой конфигурации с обязательной проверкой токена для Telegram-команд.
- `TelegramClient` поддерживает официальные методы `getUpdates` и `sendMessage`.
- Добавлены неизменяемые модели `TelegramMessage` и `TelegramUpdate` только для необходимых полей.
- `getUpdates` проверяет параметры, поддерживает long polling, безопасно разбирает JSON и игнорирует неподдерживаемые виды обновлений.
- `sendMessage` выполняет POST, проверяет HTTP-ответ и поле `ok`, а пустой текст отклоняет.
- Ошибки Telegram API не выводят токен, полный URL или полный JSON.
- Добавлен `--wait-for-start` во взаимоисключающую группу с `--check-telegram`.
- Новый обработчик сначала логически очищает накопленные обновления продвижением `offset` без ответов, затем ждёт новую команду.
- Обрабатываются только новые текстовые `/start` и `/start@weather_storm_alert_bot` из приватного чата.
- После одного успешного ответа обработчик завершается с кодом `0`.
- При `Ctrl+C` traceback не показывается, возвращается код `130`.
- Добавлен отдельный режим `--wait-for-city`, который очищает старые обновления, ждёт новую приватную `/start`, просит город, принимает один локально корректный текст из того же чата, подтверждает его и завершается.
- Реализация режима `--wait-for-city` зафиксирована в commit `27745cfbfaa27525a0bda1aae008c8c9d30ef145`.
- Город не сохраняется, его существование не проверяется; геокодирование и погодные API не вызываются.
- Автоматические тесты используют только mock и явно вымышленный токен `123456789:TEST_TOKEN_NOT_REAL`.
- Успешно прошли 40 автоматических тестов.
- `compileall` завершился без ошибок; справка CLI показывает `--wait-for-start` и `--wait-for-city`.
- Настоящий токен Codex не читал.

## Результат задачи геокодирования

- Исходный commit этой задачи: `d4f455f26c59c9097c66124ae0d0ea2e67547a33`.
- Выбран Open-Meteo Geocoding API; данные о локациях основаны на GeoNames.
- Бесплатный endpoint используется только для некоммерческого прототипа; для коммерческой эксплуатации потребуется подходящий платный тариф или замена провайдера.
- Добавлен отдельный однократный режим `--geocode-city CITY`.
- Клиент локально проверяет строку, формирует один HTTPS-запрос с максимум пятью вариантами и безопасно разбирает ответ.
- Telegram-сценарии и их тексты не изменялись; геокодирование не интегрировано в `--wait-for-city`.
- Город и координаты не сохраняются, прогноз погоды не запрашивается.
- На момент отдельной задачи CLI-геокодирования настоящий запрос к Open-Meteo ещё не выполнялся; автоматические тесты той задачи использовали mock.
- Успешно прошли 56 автоматических тестов; `compileall` завершился без ошибок.
- В рамках отдельной задачи CLI-геокодирования настоящий Telegram-токен Codex не читался и реальный Telegram-запрос не выполнялся.
- SQLite, постоянная работа, планировщик и рассылки не реализованы.

## Что подтверждено контролируемым запуском

- Перед реальным тестом рабочее дерево было чистым, локальный HEAD совпадал с `origin/main`.
- Пользователь выполнил один контролируемый реальный запуск `PYTHONPATH=src python3 -m weather_alert_bot --geocode-city "Москва"`.
- Telegram-токен для запуска не требовался и не загружался; Telegram не вызывался.
- Open-Meteo успешно вернул четыре подходящих варианта; первым вариантом была Москва, Москва, Россия.
- Первый результат содержал регион, страну, координаты и часовой пояс `Europe/Moscow`; также были возвращены другие населённые пункты с названием Москва.
- Приложение вывело не более пяти результатов и attribution Open-Meteo и GeoNames.
- Полный URL, необработанный JSON и traceback не выводились.
- Город и координаты не сохранялись; прогноз погоды не запрашивался.
- Повторный реальный запрос в рамках этой документационной задачи не выполнялся.

## Текущий stop-point

Отдельный клиент Open-Meteo Geocoding API и CLI-режим `--geocode-city CITY` созданы, проверены 56 автоматическими mock-тестами и успешно подтверждены одним контролируемым реальным запросом. Для запроса «Москва» сервис вернул четыре подходящих варианта, первым из которых была Москва в России; приложение безопасно показало регион, страну, координаты и часовой пояс с attribution Open-Meteo и GeoNames. Telegram-токен не использовался, город и координаты не сохранялись, прогноз погоды не запрашивался. Геокодирование ещё не интегрировано в Telegram. Следующий безопасный шаг — отдельная задача на интеграцию геокодирования в однократный Telegram-сценарий после ввода города, без сохранения данных и без запроса погоды.

## Актуализация после текущей задачи

- Исходный commit документационной задачи: `56dbf8d3e2c2cdf4ef35c96ae72349302b03fd0d`.
- Добавлен отдельный однократный режим `--wait-for-geocoded-city`; существующие `--check-telegram`, `--wait-for-start`, `--wait-for-city` и `--geocode-city CITY` сохранены.
- Новый режим очищает старые обновления, ждёт новую приватную `/start`, просит город, локально проверяет текст, выполняет один запрос через Open-Meteo и отправляет первый результат с названием, регионом при наличии, страной, координатами, часовым поясом и attribution Open-Meteo и GeoNames.
- 5 августа 2026 года один контролируемый реальный запуск `PYTHONPATH=src python3 -m weather_alert_bot --wait-for-geocoded-city` успешно выполнен: Telegram-токен был безопасно загружен оболочкой из защищённого файла окружения и не выводился.
- Новая приватная `/start` от пользователя была распознана; бот попросил город, принял «Москва», выполнил реальный запрос к Open-Meteo Geocoding API и отправил первый результат: Москва, регион Москва, Россия, координаты `55.752040, 37.617810`, часовой пояс `Europe/Moscow`.
- В отправленном результате присутствовал attribution Open-Meteo и GeoNames; программа штатно завершилась после отправки результата.
- Токен, chat_id, URL, JSON, название города и координаты в терминал не выводились. Город и координаты не сохраняются, прогноз погоды не запрашивается, API-ключ Open-Meteo не используется.
- Ветки отсутствия совпадений, ошибки Open-Meteo, некорректного текста, Ctrl+C, другого чата и групповых сообщений реально не проверялись; они остаются подтверждёнными только автоматическими mock/fake-тестами.
- До этого реального запуска ранее Codex сообщил об успешном прохождении 66 автоматических тестов. В этой документационной задаче тесты не запускались.
- Настоящий токен Codex не читал. SQLite, постоянный polling, systemd, планировщик и рассылки не реализованы.

### Текущий stop-point

Интеграция Telegram-ввода города и Open-Meteo подтверждена одним контролируемым реальным тестом: пользователь отправил новую приватную `/start`, затем «Москва», бот вернул первый найденный вариант с регионом, страной, координатами `55.752040, 37.617810`, часовым поясом `Europe/Moscow` и attribution Open-Meteo и GeoNames, после чего штатно завершился. Данные не сохраняются, прогноз не запрашивается. Успешный реальный сценарий подтверждён; остальные ветки остаются подтверждёнными автоматическими mock/fake-тестами. Текущий этап подтверждён и задокументирован. Следующий функциональный этап не выполнять автоматически; он должен быть определён отдельной задачей после проверки актуального технического задания и roadmap.

## Что намеренно не реализовано

- Постоянный polling не реализован.
- systemd-сервис не создан.
- Webhook не создан.
- Полный выбор города, его сохранение и проверка существования не реализованы; однократные сценарии существуют только в `--wait-for-city` и `--geocode-city`.
- Геокодирование интегрировано только в однократный режим `--wait-for-geocoded-city`; город и координаты не сохраняются.
- Погодные API, NOAA, SQLite, расписание, планировщик, предупреждения и рассылки не реализованы.
- Геолокация и кнопки Telegram не добавлены.
- systemd не создан.
- Сторонние зависимости не добавлены.
- `next_steps.md` не создан.

## Актуализация после текущей задачи

- Исходный commit задачи: `87bd639e857e645ce795c41274f2ffd3a03ba586`.
- Добавлен отдельный однократный режим `--wait-for-confirmed-city`; существующий `--wait-for-geocoded-city` сохранён без изменения пользовательского поведения.
- Новый сценарий очищает старые Telegram-обновления, ждёт новую приватную `/start`, просит город, локально проверяет его, выполняет один запрос Open-Meteo, показывает первый найденный вариант и спрашивает «Да» или «Нет».
- При «Да» город подтверждается и сценарий завершается; при «Нет» бот просит другой город без повторной `/start`. Некорректный ответ вызывает повторный запрос «Да» или «Нет».
- Город, координаты и часовой пояс не сохраняются; прогноз погоды не запрашивается, API-ключ не используется.
- Все новые сценарии проверены только fake/mock-тестами. Настоящий токен Codex не читался, реальный запуск нового режима ещё не выполнялся.
- SQLite, постоянный polling, systemd, расписание и рассылки не реализованы.

### Текущий stop-point

Добавлен отдельный однократный режим `--wait-for-confirmed-city`. Он очищает старые Telegram-обновления, ждёт новую приватную `/start`, просит город, выполняет геокодирование, показывает первый результат и ждёт ответа «Да» или «Нет» из того же чата. При «Да» подтверждает город и завершается, при «Нет» просит другой город без повторной `/start`. Никакие данные не сохраняются, прогноз не запрашивается. Новый сценарий проверен только fake/mock-тестами. Следующий безопасный шаг — после независимой проверки commit провести один контролируемый реальный тест `--wait-for-confirmed-city`.
